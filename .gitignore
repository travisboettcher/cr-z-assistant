node_modules
dist
coverage

# Scratch checkouts created when stories are built in parallel agent worktrees.
.claude/worktrees/
*.local
.DS_Store
.vite
playwright-report
test-results

# Mutation testing: Stryker's sandbox and its generated report.
.stryker-tmp
reports
