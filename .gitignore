node_modules
dist
coverage

# Scratch checkouts created when stories are built in parallel agent worktrees.
.claude/worktrees/
*.local
.DS_Store
.vite
playwright-report
test-results

# Stryker's mutation report; the score that matters is the one CI prints.
reports
